# 🐼 คู่มือการเขียน Pandas สำหรับ Umi

Umi เมื่อต้องสรุปยอดบัญชี คุณต้องเขียนสคริปต์ตามมาตรฐานนี้:

## 1. การเชื่อมต่อฐานข้อมูล (Connection)
ใช้ SQLAlchemy เชื่อมต่อกับ sqlite:////data/data/com.termux/files/home/.openclaw/workspace/hotel_account.db

## 2. ขั้นตอนการประมวลผล (Processing)
1. **Load:** อ่านตาราง  หรือ  เข้ามาใน DataFrame
2. **Clean:** แปลงคอลัมน์วันที่ด้วย  และจัดการค่าว่าง
3. **Filter:** กรองข้อมูลตามช่วงวันที่ที่ผู้ใช้ต้องการ (เช่น '2024-02')
4. **Aggregate:** ใช้  สำหรับยอดเงิน และ  สำหรับจำนวนห้อง

## 3. การรายงานผล (Reporting)
- แสดงผลสรุปเป็นตาราง Markdown ในแชท
- หากข้อมูลมีขนาดใหญ่ ให้บันทึกเป็นไฟล์ .csv ในโฟลเดอร์  แล้วแจ้ง Path ให้ผู้ทราบ