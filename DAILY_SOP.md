# 📋 คู่มือปฏิบัติงานประจำวัน (Daily SOP) — วิพัฒน์โฮเทล

> ขั้นตอนมาตรฐานสำหรับการจัดการบัญชีและฐานข้อมูลประจำวัน เพื่อให้ข้อมูลแม่นยำ 100%

---

## ☀️ ช่วงเช้า: ตรวจสอบสถานะระบบ
1. **ตรวจสอบความพร้อมของฐานข้อมูล**:
   - ตรวจเช็กว่าไฟล์ `hotel_account.db` อยู่ในตำแหน่งที่ถูกต้อง (Root Directory)
   - รัน Health Check เบื้องต้นผ่าน `fix_database.py` เพื่อดูจำนวนห้องว่าง
2. **อัปเดตสถานะห้องพัก**:
   - หากมีการแจ้งห้องเสียหรือซ่อมบำรุง ให้บันทึกสถานะเป็น `Under Maintenance`
   - ตรวจสอบห้องรายเดือน (`Monthly`) ว่ายังพักอยู่ตามปกติหรือไม่

## 🧾 ระหว่างวัน: บันทึกรายการ (Income/Expense)
เมื่อมีการจองหรือค่าใช้จ่ายเกิดขึ้น ให้ใช้ฟังก์ชันใน `fix_database.py` หรือพิมพ์คำสั่ง SQL โดยตรง:
- **บันทึกรายรับ**: `add_income(date, source, amount, note)`
- **บันทึกรายจ่าย**: `add_expense(date, category, amount, vendor)`
*หมายเหตุ: หากรับสลิปโอนเงิน ให้ส่งให้ Umi วิเคราะห์และบันทึกเข้าระบบทันที*

## 🌙 ช่วงเย็น: สรุปผลและทำความสะอาดข้อมูล
1. **รัน Script แก้ไขและทำความสะอาด**:
   ```bash
   python3 fix_database.py
   ```
   *เพื่อซิงค์ข้อมูลจาก Excel (ถ้ามีการนำเข้า) และสร้างตาราง `clean_transactions` ที่สมบูรณ์*
2. **ตรวจสอบ Dashboard**:
   - เปิดไฟล์ `Dashboard.html` เพื่อดูภาพรวมรายได้ประจำวัน
   - ตรวจสอบว่า "ห้อง Hero" (B106, B107, A105) มีความเคลื่อนไหวอย่างไร
3. **สำรองข้อมูล (Backup)**:
   - คัดลอก `hotel_account.db` เก็บไว้ในโฟลเดอร์สำรองสัปดาห์ละครั้ง

---

## ⚠️ ข้อควรระวัง
- **อย่าแก้ไขชื่อคอลัมน์ใน SQL โดยไม่ปรึกษา Umi**: เพราะจะทำให้ Script พัง (โดยเฉพาะเรื่อง `room_no` vs `room_number`)
- **การนำเข้าไฟล์ Excel**: หลังนำเข้าเสร็จแล้ว ให้รัน `fix_database.py` เสมอเพื่อตรวจสอบ Column Misalignment

---
*DAILY_SOP.md | วิพัฒน์โฮเทล | ฉบับฟื้นฟูและอัปเดตล่าสุด 27/02/2026*
